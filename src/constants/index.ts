export const IMAGE_URL = 'https://picsum.photos/v2/list';
export const IMAGES_LOCAL_STORAGE_KEY = 'imagesRotationState';

export enum PositionDegree {
  Angle = 90,
  Init = 0,
}

export enum LoadingState {
  Loading = 'LOADING',
  Success = 'SUCCESS',
  Failed = 'FAILED',
}
